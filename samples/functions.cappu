//Cappuccino
//
//functions

//Functions are functions they are simple, just like they should be
function hello(number)
    print("hi " + number);
end

hello(5)

//They can also be declared like JS functions.

const jsHello = () => {
    print("hi")
}

jsHello();
